<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miniprojetandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.UpdateProfile"></activity>
        <activity android:name=".ui.activities.AddBike" />
        <activity android:name=".ui.activities.FindActivity" />
        <activity android:name=".ui.activities.TrackActivity" />
        <activity android:name=".ui.activities.RentActivity" />
        <activity android:name=".ui.activities.MainActivityAdmin" />
        <activity android:name=".ui.activities.SignUpActivity" />
        <activity android:name=".ui.activities.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.MainActivity" />
    </application>

</manifest>